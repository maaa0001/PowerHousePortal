services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: power_house_portal_web_server_router
    depends_on:
      - mariadb
    ports:
      - 80:80 # 0 - 65535 from 0 to 1024 are reserved 1025 and above 655535
    volumes:
      - .:/var/www/html

  mariadb:
    image: mariadb:10.6.20
    container_name: power_house_portal_mariadb
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: power_house_portal
    volumes:
      - mariadb_data:/var/lib/mysql
    ports:
      - 3306:3306

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: power_house_portal_phpmyadmin
    depends_on:
      - mariadb
    environment:
      PMA_HOST: mariadb
      MYSQL_ROOT_PASSWORD: password
    ports:
      - 8080:80
volumes:
  mariadb_data:
